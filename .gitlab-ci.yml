image: node:6.10-alpine

pages:
  cache:
    paths:
    - node_modules/

  script:
  - npm install
  - export PATH="$PATH:node_modules/.bin"
  - npm build
  artifacts:
    paths:
    - public
  only:
  - master
