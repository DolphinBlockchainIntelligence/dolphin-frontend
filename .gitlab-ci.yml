image: node:6.10-alpine

before_script:
  - npm install

stages:
  - build

build:
  stage: build
  cache:
    paths:
    - node_modules/
  script:
  - npm run build
  artifacts:
    paths:
    - public
  only:
  - master
